import React from 'react';
import { shallow } from 'enzyme';

import LanguageSwitch from './LanguageSwitch.jsx';

describe('Test for LanguageSwitch', () => {
  it('', () => {
    const wrapper = shallow(<LanguageSwitch />);
  });
});
