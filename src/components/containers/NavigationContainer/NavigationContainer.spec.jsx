import React from 'react';
import { shallow } from 'enzyme';

import NavigationContainer from '/NavigationContainer.jsx';

describe('Test for NavigationContainer component', () => {
  it('', () => {
    const wrapper = shallow(<NavigationContainer />);
  });
});
