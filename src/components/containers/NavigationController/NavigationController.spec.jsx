import { shallow } from 'enzyme';

import NavigationController from '/NavigationController.jsx';

describe('Test for NavigationController component', () => {
  it('', () => {
    const wrapper = shallow(<NavigationController />);
  });
});
