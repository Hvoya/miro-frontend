import React from 'react';
import { shallow } from 'enzyme';

import NavigationStructure from './NavigationStructure.jsx';

describe('Test for NavigationStructure', () => {
  it('', () => {
    const wrapper = shallow(<NavigationStructure />);
  });
});
