import { shallow } from 'enzyme';

import BaseTemplateStructure from '/BaseTemplateStructure.jsx';

describe('Test for BaseTemplateStructure component', () => {
  it('', () => {
    const wrapper = shallow(<BaseTemplateStructure />);
  });
});
